# Setting up WOFI

Ensure installed:
- **`wofi`**
- `sassc`

## Setup
Symlink this folder with `~/.config/wofi`.

```bash
rm -rf '/home/kalpakavindu/.config/wofi'
sudo ln -s -n '/home/kalpakavindu/Source/dotfiles/linux/wofi' '/home/kalpakavindu/.config/wofi'
```