# Setting up alacritty

Ensure installed:
- `alacritty`

## Setup

Symlink tis folder with `~/.config/alacritty`

```bash
ln -sf -n '/home/kalpakavindu/Source/dotfiles/linux/alacritty' '/home/kalpakavindu/.config/alacritty'
```